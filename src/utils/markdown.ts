// Markdown utility functions for future implementation

export async function parseMarkdown(content: string): Promise<string> {
  // TODO: Implement markdown parsing
  // Consider using libraries like 'marked' or 'remark'
  return content;
}

export async function getMarkdownFiles(directory: string): Promise<string[]> {
  // TODO: Implement markdown file discovery
  // This will help load content from /content directory
  return [];
}
